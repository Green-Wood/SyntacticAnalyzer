package yacc

import kotlin.random.Random
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue


internal class GOTOGraphTest {
    @Test
    fun testInitGraph() {
//        val grammarString = "{E} -> {E} * {E} | {E} + {E} | ( {E} ) | id"
//        val grammar = Grammar(grammarString)
//        val graph = GOTOGraph(grammar)
//        assertEquals(18, graph.idSetMap.values.size)
//        assertEquals(37, graph.adjacent.values.flatten().size)
    }
}